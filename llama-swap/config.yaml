# AUTO-GENERATED from models.conf by scripts/generate-config.sh.
# Do not edit by hand - your changes will be overwritten.
# Regenerate with:  bash scripts/generate-config.sh
# Apply with:       docker compose restart turboquant

startPort: 5800
sendLoadingState: false
globalTTL: 0

macros:
  engine: "/app/llama-server"
  fit_target: "512"

models:
  "deepseek-v4-flash-64k":
    name: "DeepSeek V4 Flash - 64K"
    description: "DeepSeek V4 Flash 0731 (284B MoE, 21B active, MXFP4 routed experts). CPU-MoE: all 137 GiB routed experts run on CPU, ~13.7 GiB BF16 backbone on GPU. FP16 KV mandatory for V4 arch (quantized KV silently corrupts). Slow decode on 16 GB GPU."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/DeepSeek-V4-Flash-0731-UD-Q8_K_XL/UD-Q8_K_XL/DeepSeek-V4-Flash-0731-UD-Q8_K_XL-00001-of-00005.gguf"
      -fit on -fitt ${fit_target}
      -c 65536
      -ctk f16 -ctv f16
      --flash-attn on --jinja -np 1
      -cmoe
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 65536
  "qwen3.5-9b-32k":
    name: "Qwen3.5 9B - 32K"
    description: "Smoke-test / lightweight profile. q8_0 K + turbo3 V."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.5-9B-Q4_K_M.gguf"
      -fit on -fitt ${fit_target}
      -c 32768
      -ctk turbo2 -ctv turbo3
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 32768
  "qwen3.6-35b-a3b-250k":
    name: "Qwen3.6 35B A3B - 250K"
    description: "MoE daily driver: 3B active, DeltaNet attention (tiny KV cache), 250K context. Spill handled via --moe-cache soft."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.6-35B-A3B-UD-Q4_K_M.gguf"
      -fit on -fitt ${fit_target}
      -c 250000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
      --moe-cache soft
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 250000
  "qwen3.6-35b-a3b-bf16-250k":
    name: "Qwen3.6 35B A3B - BF16 250K"
    description: "Full-precision BF16 (2-part split). MoE cache does NOT support BF16 matvec, so decode streams experts over PCIe (slow decode, best quality)."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.6-35B-A3B-BF16-00001-of-00002.gguf"
      -fit on -fitt ${fit_target}
      -c 250000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 250000
  "qwen3.6-35b-a3b-q6-250k":
    name: "Qwen3.6 35B A3B - Q6_K 250K"
    description: "Mid-quality 6-bit UD variant, MoE cache enabled."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.6-35B-A3B-UD-Q6_K.gguf"
      -fit on -fitt ${fit_target}
      -c 250000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
      --moe-cache soft
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 250000
  "qwen3.6-35b-a3b-q8-250k":
    name: "Qwen3.6 35B A3B - Q8_0 250K"
    description: "High-quality 8-bit variant (non-UD), MoE cache enabled."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.6-35B-A3B-Q8_0.gguf"
      -fit on -fitt ${fit_target}
      -c 250000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
      --moe-cache soft
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 250000
  "qwen3.8-27b-128k":
    name: "Qwen3.8 27B - 128K"
    description: "Long context, ~15 tok/s decode."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.8-27B-UD-Q3_K_XL.gguf"
      -fit on -fitt ${fit_target}
      -c 131072
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 131072
  "qwen3.8-27b-200k":
    name: "Qwen3.8 27B - 200K"
    description: "Maximum-context profile, ~11 tok/s decode."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.8-27B-UD-Q3_K_XL.gguf"
      -fit on -fitt ${fit_target}
      -c 200000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 200000
  "qwen3.8-27b-250k":
    name: "Qwen3.8 27B - 250K"
    description: "Absolute maximum context, ~10 tok/s decode."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.8-27B-UD-Q3_K_XL.gguf"
      -fit on -fitt ${fit_target}
      -c 250000
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 250000
  "qwen3.8-27b-64k":
    name: "Qwen3.8 27B - 64K"
    description: "Daily driver: full GPU residency, ~23 tok/s decode."
    cmd: |
      "${engine}" --host 127.0.0.1 --port ${PORT}
      --alias ${MODEL_ID}
      -m "/models/gguf/Qwen3.8-27B-UD-Q3_K_XL.gguf"
      -fit on -fitt ${fit_target}
      -c 65536
      -ctk turbo2 -ctv turbo2
      --flash-attn on --jinja -np 1
    capabilities:
      in: [text]
      out: [text]
      tools: true
      context: 65536
